language: csharp

dotnet: 2.1.4
mono: 5.12.0
dist: trusty

env:
 - HOME=/home/travis APPDATA=/home/travis LocalAppData=/home/travis
 
sudo: false

before_install:
  - chmod +x build.sh

script:
  - ./build.sh
